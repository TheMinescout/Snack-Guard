name = "snackguard-worker"
main = "cloudflare-worker.js"
compatibility_date = "2024-01-01"

# Bind Workers AI
[ai]
binding = "AI"

# Optional: Add rate limiting to prevent abuse
# [[unsafe.bindings]]
# name = "RATE_LIMITER"
# type = "ratelimit"
# namespace_id = "your_namespace_id"
# simple = { limit = 100, period = 60 }
